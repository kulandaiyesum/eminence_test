export class Qbank {
  // _id: string;
  // systemId?: string;
  // subsystemId?: string;
  // subjectId?: string;
  type: number; // (0 or 1 or 2 or 3 or 4)
  mode: string; //(tutor or timed)
  userId: string;
  questionsCount: number;
  status: string;
}
